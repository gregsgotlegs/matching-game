import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'matching-game',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
